import React, {PropTypes} from 'react-native';
import PropTypes from 'prop-types';
import {View, Text, TouchableHighlight, TextInput} from 'react';
import {styles} from './styles';

const inputWithButton = () => (

);
  